import * as React from 'react';
import AddNewCourse from '../../../feature-courses/ui/components/AddNewCourse';
import 'ignore-styles';

describe('courseRepositoryCheck', () => {

    it('addToDatabase', async () => {

        expect(result.isSuccess).toBe(true);
    });

})